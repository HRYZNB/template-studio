[project]
name = "ruiware-mcp"
version = "0.1.0"
description = "Local MCP bridge for the RuiWare part-template platform"
requires-python = ">=3.11"

[project.scripts]
ruiware-mcp = "ruiware_mcp.server:main"

[tool.pytest.ini_options]
pythonpath = ["."]
